import Foundation

enum AppConfig {
    // Troque por uma URL HTTPS real com o JSON da família antes do TestFlight.
    static let videoLibraryURL = URL(string: "https://example.com/gatoteca/videos.json")!
}
